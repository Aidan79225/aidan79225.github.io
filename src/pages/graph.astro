---
import BaseLayout from '../layouts/BaseLayout.astro';
import Graph from '../components/Graph.jsx';
---
<BaseLayout
  title="關係圖"
  description="這個部落格的文章關係圖 —— 用一張力導向圖把文章之間的引用連線畫出來,看得到系列聚落與樞紐文章。"
>
  <h1 class="text-3xl font-bold mb-2">文章關係圖</h1>
  <p class="text-muted mb-4">
    每個點是一篇文章,連線是文章之間的引用。顏色代表系列,點越大代表被連得越多(越像樞紐)。拖曳可移動、滾輪縮放、點一下節點就跳到那篇。
  </p>
  <Graph client:only="react" />
  <p class="text-muted text-sm mt-4">
    想照順序讀?到 <a href="/start/" class="text-accent hover:underline">Start Here</a> 看推薦路徑;想找主題,用右上角搜尋。
  </p>
</BaseLayout>
